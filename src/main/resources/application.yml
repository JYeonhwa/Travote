server:
  port: 8000
  servlet:
    context-path: /

spring:
  application:
    name: server
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://3.34.233.129:3306/travote?serverTimezone=Asia/Seoul
    username: travote
    password: travote3306!
  jpa:
    show_sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create

